% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/12_auto_bvr.R
\name{auto_bvr}
\alias{auto_bvr}
\title{Automated Model Selection for mixLCA}
\usage{
auto_bvr(
  data,
  continuous = NULL,
  categorical = NULL,
  concomitant = NULL,
  K_range = 2:5,
  max_direct_effects = 5L,
  bvr_threshold = 3.84,
  seed = 110L,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{data}{Data frame.}

\item{continuous}{Character vector of continuous indicator names, or NULL.}

\item{categorical}{Character vector of categorical indicator names, or NULL.}

\item{concomitant}{Character vector or formula of concomitant predictors,
or NULL.}

\item{K_range}{Integer vector of class counts to evaluate (e.g., 2:5).}

\item{max_direct_effects}{Integer: maximum number of categorical direct
effects to add during Phase 2b (default 5).}

\item{bvr_threshold}{Numeric: minimum BVR chi-squared statistic to
consider a direct effect (default 3.84, i.e. p < .05 for df = 1).}

\item{seed}{Random seed.}

\item{verbose}{Logical: print progress?}

\item{...}{Additional arguments passed to \code{fit_lca} (e.g.,
\code{spectral_rank}, \code{spectral_pool}).}
}
\value{
An object of class \code{mixLCA} representing the final selected
  model, with an additional \code{auto_path} element recording the
  search trajectory.
}
\description{
Three-phase automated model selection:
\describe{
  \item{Phase 1}{Select \emph{K} by BIC across \code{K_range} using
    diagonal covariance.}
  \item{Phase 2a}{Compare diagonal, penalised, and full covariance
    structures for continuous indicators at the selected \emph{K}.}
  \item{Phase 2b}{Vermunt-style BVR-guided specification search for
    categorical indicators: iteratively add direct effects for the
    pair with the largest bivariate residual, stopping when BIC no
    longer improves or \code{max_direct_effects} is reached.}
}
}
\details{
Parallel execution of multiple starts is delegated to the user's active
\code{future::plan()} (set it in the session before calling this function).
}
\examples{
\dontrun{
data(voter_perceptions)
fit <- auto_bvr(
  data        = voter_perceptions,
  categorical = names(voter_perceptions),
  K_range     = 3,
  max_direct_effects = 4L,
  seed        = 110L,
  n_starts    = 3,
  verbose     = FALSE)
fit$auto_path$direct_effects
fit_indices(fit)$BIC
}
}
